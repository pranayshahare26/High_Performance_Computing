*******
* GDB *
*******

ulimit -c unlimited						enable core dumps
gdb PROGRAM core						start from core dump
gdb PROGRAM PROCESS_ID						attach to running process
r								run program
bt								backtrace
up/down/frame N							go up/down in call stack	
n								step over
s								step into
fin								return from current subroutine
Ctrl-X Ctrl-A							switch to full-screen user interface
b [FILE_NAME:]LINE_NUMBER [thread N] [if COND]			set break point
b FUNCTION_NAME [thread N] [if COND]	

*********
* GProf *
*********
	Command									Meaning
# gcc -pg -o my-program my-program.c				Compile with instrumentation for gprof

# ./my-program							Run the program, writes gmon.out.

# gprof ./my-pogram gmon.out [FURTHER OPTIONS]			Examine profiler output



***************
* IntelOneapi *
***************

# source /opt/intel/oneapi/setvars.sh
	For load oneapi
# mpicc <program_name>
	For compiling
# mpirun -np (processor) aps ./a.out
	For run and execute give profiling

****************************************************
* Linux Application Installation and HPC Libraries *
****************************************************

FOr RPM packages

# rpm -i RPMPackage.rpm
	Perform the extraction of files by already defined option
	For Software installation

# yum update
# yum install Package
# yum remove Package
	For update, install pacakges & remove pcakages

For debian packages

# dpkg -i Debianpackages.deb
	For packages installation

# apt-get update
# apt-get upgrade
# apt-cache search package
# apt-get install package
# apt-get remove package
	Common command we used

For source code installation

Step 1 :-

	# wget "<download_link>"
		For command line get file 

	# tar -xvf tarBall.tar.gz
	# tar -zxvf TarBall.tar.gz
	# tar -jxvf tarBall.tar.bz2
	# inzip TarBall.zip
		For archives exatract file

Step 2 :-
	
	# cd PackageDirectory
		Goto extract packaged
	# ./configure
		Chack for any neccessory libraries

Step 3 :-
	
	# make
		For compile the source code, which read a rule file
	# make check
		For verify the build software

Step 4 :-

	# make install
		For installation packages

Step 5 :-
	
	# echo $?
		For last command successfully run or execute to check

